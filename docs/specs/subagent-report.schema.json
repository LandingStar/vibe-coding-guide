{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "subagent-report.schema.json",
  "title": "Subagent Report",
  "description": "Platform-level schema for a subagent report. See docs/subagent-schemas.md for full semantics, invariants, and customization boundaries.",
  "type": "object",
  "required": [
    "report_id",
    "contract_id",
    "status",
    "changed_artifacts",
    "verification_results"
  ],
  "properties": {
    "report_id": {
      "type": "string",
      "description": "Unique identifier for the report. Must start with 'report-'.",
      "pattern": "^report-.+"
    },
    "contract_id": {
      "type": "string",
      "description": "Identifier of the corresponding contract. Must start with 'contract-'.",
      "pattern": "^contract-.+"
    },
    "status": {
      "type": "string",
      "description": "Execution outcome status.",
      "enum": ["completed", "partial", "blocked"]
    },
    "changed_artifacts": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Artifacts actually modified during execution. Should not be vague descriptions."
    },
    "artifact_payloads": {
      "type": "array",
      "items": { "$ref": "#/$defs/artifactPayload" },
      "description": "Optional structured content payloads for future writeback planning. Presence does not imply the payload has already been written back."
    },
    "verification_results": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Verification steps executed and their results. Should not be empty descriptions."
    },
    "unresolved_items": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Open items that remain unresolved after execution."
    },
    "assumptions": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Assumptions made during execution that the supervisor should verify."
    },
    "escalation_recommendation": {
      "type": "string",
      "description": "Whether escalation is recommended and to whom.",
      "enum": ["none", "review_by_supervisor", "human_review"]
    }
  },
  "$defs": {
    "artifactPayload": {
      "type": "object",
      "required": ["path", "content", "operation", "content_type"],
      "properties": {
        "path": {
          "type": "string",
          "description": "Target artifact path associated with this payload.",
          "minLength": 1
        },
        "content": {
          "type": "string",
          "description": "Structured content candidate for future writeback consumption."
        },
        "operation": {
          "type": "string",
          "description": "First-pass writeback intent carried by the report payload.",
          "enum": ["create", "update", "append"]
        },
        "content_type": {
          "type": "string",
          "description": "Declared content type for downstream writeback mapping.",
          "enum": ["markdown", "json", "yaml", "text"]
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
